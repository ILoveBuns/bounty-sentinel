{
  "name": "bounty-sentinel",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json && vite build web --outDir ../dist-web --emptyOutDir",
    "test": "vitest run",
    "check": "npm run build && npm test",
    "demo": "tsx src/demo.ts",
    "dashboard": "vite web --host 0.0.0.0",
    "sanitize-receipt": "tsx src/receipt-cli.ts"
  },
  "dependencies": {
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0",
    "vite": "^7.0.0",
    "vitest": "^3.2.0"
  }
}
