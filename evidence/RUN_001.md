# Real Scraper Studio run 001

## Scope

- Started after the organizer declared Into the Scrape-Verse live.
- Public target: `https://github.com/FreeCAD/FreeCAD/issues/18969`
- Requested fields: issue title, status, labels, assignees, and related pull-request links.
- Collector: `c_msxcahpj2ik5c4hsoi`

## Observed result

The Scraper Studio create command reached intent analysis, output-schema generation, code generation, and preview attempts, but did not finish within the official CLI's 600-second polling limit. The returned envelope status is `poll_failed`. A direct run then failed closed with HTTP 403 because the collector did not have a completed template.

No output from this collector is treated as opportunity data. It does not enter ranking, cash totals, or the dashboard's live evidence stream.

## Cost boundary

The CLI reported `$52.00` balance and `$0.00` pending charge before and after this attempt. No paid plan or payment method was added.

## Public evidence

- Sanitized create receipt: `receipts/freecad-18969-create.public.json`
- Raw CLI envelopes remain in ignored `receipts/private/` with restricted local permissions.
- The public receipt binds the private envelope by SHA-256 without publishing credentials, account identifiers, cookies, or raw page bodies.

## Recovery decision

The half-built collector remains isolated. The next attempt must either complete its template in Scraper Studio or create a replacement collector with a narrower extraction request. A successful output must pass schema and evidence validation before publication.
